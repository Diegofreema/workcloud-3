import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const PendingStaffs = (props: Props) => {
  return (
    <View>
      <Text>PendingStaffs</Text>
    </View>
  );
};

export default PendingStaffs;

const styles = StyleSheet.create({});
